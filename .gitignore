# 各パッケージ配下にも .gitignore はあるが、ルート直下の取りこぼしを防ぐための保険
node_modules
dist
.env
.DS_Store

# Claude Codeのローカル設定(このマシン固有の許可設定などを含むため共有しない)
.claude/settings.local.json

# 未来リストのアプリとは無関係なファイル(PC上には残すが、GitHubの管理下からは外す)
backend/todo-app/
fukuoka_weather.html
fukuoka_weather.py
index.html
